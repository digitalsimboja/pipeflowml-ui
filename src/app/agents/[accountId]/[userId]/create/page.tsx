import Sidebar from "src/components/Sidebar";

const CreateAgent = () => {
    return (
      <Sidebar>
        <div>Create new agent</div>
      </Sidebar>
    );
  };
  
  export default CreateAgent;